#!hexa strict
// install.hexa — hx build hook for hexa-matter.
//

fn run_quick_selftest(root: str) -> bool {
    let cli = root + "/cli/hexa-matter.hexa"
    let exists = exec("[ -f \"" + cli + "\" ] && echo y || echo n").trim()
    if exists != "y" {
        eprintln("[hexa-matter/install] WARN: cli/hexa-matter.hexa missing — skip selftest")
        return false
    }
    println("[hexa-matter/install] [post] running quick selftest (16-verb count check)")
    let cmd = "HEXA_MATTER_ROOT=\"" + root + "\" hexa run \"" + cli + "\" selftest 2>&1"
    let raw = exec(cmd)
    if raw.contains("__HEXA_MATTER_SELFTEST__ PASS") {
        println("[hexa-matter/install]   selftest PASS — 16/16 verbs present")
        return true
    }
    eprintln("[hexa-matter/install] WARN: selftest non-PASS (warn-only)")
    return false
}

fn main() -> void {
    let mut root = env("HX_PKG_DIR")
    if root == "" { root = exec("pwd").trim() }
    let phase = env("HX_HOOK_PHASE")
    let mut p = phase
    if p == "" { p = "both" }
    println("[hexa-matter/install] HX_PKG_DIR=" + root + " phase=" + p)

    let entry = root + "/cli/hexa-matter.hexa"
    let entry_ok = exec("[ -f \"" + entry + "\" ] && echo y || echo n").trim()
    if entry_ok != "y" {
        eprintln("[hexa-matter/install] ERROR: canonical entry missing: " + entry)
        exit(1)
    }
    println("[hexa-matter/install] canonical entry OK: cli/hexa-matter.hexa")

    if p == "pre" || p == "both" {
        println("[hexa-matter/install] [pre] no external deps (pure spec substrate) — skip")
    }
    if p == "post" || p == "both" {
        let _ = run_quick_selftest(root)
    }
    println("[hexa-matter/install] done")
}
